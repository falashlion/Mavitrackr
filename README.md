instructions to pull this project 

- clone the kpi directory to the environment to be used
- open the kpi project and in it the terminal run the command ./vendor/bin/sail up  to to start the Docker containers 
- make the database migrations by running the command php artisan migrate.

